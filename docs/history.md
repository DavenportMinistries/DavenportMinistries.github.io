# Davenport Ministries — Shipped History

Completed milestones, condensed. Full specs live in git history.

---

## Initial Repo Setup

GitHub Pages repo created with custom CNAME (`davenportministries.com`) and initial README. Foundation for the site is in place.

## Site Launch — Jekyll Scaffold + Content

Shipped the full single-page site: Jekyll scaffold (`Gemfile`, `_config.yml`, `_layouts/default.html`), custom CSS with sticky nav, hero, About, and Projects sections. Cards for Fellowship App (fellowship-app.com) and SDA Match (sdamatch.app). Domain wired in `_config.yml`. Site deploys automatically on push to `main`.
